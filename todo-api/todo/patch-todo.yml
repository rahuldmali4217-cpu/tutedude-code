info:
  name: patch-todo
  type: http

http:
  method: PATCH
  url: http://localhost:5000/api/todos/6a360132186507cb956596f2/status
  body:
    type: json
    data: |-
      {
        "status": "Completed"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
